<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Decrypt Hisab - Khaatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body class="bg-gradient-to-br from-pink-100 via-purple-100 to-blue-100 text-gray-800 font-sans">
    <main class="w-full min-h-screen flex flex-col items-center px-6 py-10">
        
        <!-- Navbar -->
        <nav class="w-full max-w-5xl flex items-center justify-between px-6 py-4 bg-white shadow-lg rounded-lg">
            <h3 class="text-3xl font-bold text-purple-600">Khaatabook</h3>
            <a class="text-gray-500 hover:text-purple-500 transition" href="/">← Back to Home</a>
        </nav>

        <!-- Decrypt Hisab Form -->
        <div class="mt-10 w-full max-w-3xl p-6 bg-white rounded-lg shadow-lg">
            <h2 class="text-2xl font-semibold tracking-tight mb-6 text-purple-600">Enter Password to View Hisab</h2>

            <!-- Error Message -->
            <% if (error) { %>
                <div class="mb-4 p-3 bg-red-100 text-red-600 text-sm rounded-lg flex items-center">
                    <i class="ri-error-warning-line mr-2"></i> <%= error %>
                </div>
            <% } %>

            <form action="/decrypt/<%= filename %>" method="POST" class="space-y-4">
                <!-- Password Input -->
                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Password</label>
                    <input type="password" id="password" name="password" required
                        class="w-full px-4 py-2 bg-gray-100 text-gray-800 border border-gray-300 rounded-lg focus:ring focus:ring-purple-300 outline-none"
                        placeholder="Enter decryption password..." />
                </div>

                <!-- Submit Button -->
                <button type="submit"
                    class="w-full px-4 py-2 bg-purple-500 hover:bg-purple-400 text-white font-semibold rounded-lg transition-all hover:ring-4 hover:ring-purple-300">
                    <i class="ri-lock-unlock-line mr-2"></i> Unlock Hisab
                </button>
            </form>
        </div>
    </main>
</body>
</html>