<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup - Khaatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>
<body class="bg-gradient-to-br from-pink-100 via-purple-100 to-blue-100 flex items-center justify-center min-h-screen">
    
    <div class="bg-white p-8 rounded-lg shadow-xl w-96 backdrop-blur-lg border border-gray-200">
        <h2 class="text-3xl font-bold text-center mb-6 text-purple-500">Create an Account</h2>

        <!-- Error Message (If Any) -->
        <% if (error) { %>
            <div class="mb-4 p-3 bg-red-100 text-red-600 text-sm rounded-lg flex items-center">
                <i class="ri-error-warning-line mr-2"></i> <%= error %>
            </div>
        <% } %>

        <form action="/signup" method="POST" class="space-y-4">
            <div class="relative">
                <input type="text" name="username" placeholder="Username" required 
                    class="w-full p-3 pl-10 bg-gray-50 border border-gray-300 text-gray-800 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-400 shadow-sm">
                <i class="ri-user-line absolute left-3 top-3 text-gray-400"></i>
            </div>
            
            <div class="relative">
                <input type="password" name="password" placeholder="Password" required 
                    class="w-full p-3 pl-10 bg-gray-50 border border-gray-300 text-gray-800 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-400 shadow-sm">
                <i class="ri-lock-line absolute left-3 top-3 text-gray-400"></i>
            </div>

            <button type="submit" 
                class="w-full bg-purple-500 hover:bg-purple-400 transition duration-300 ease-in-out transform hover:scale-105 text-white font-bold py-3 rounded-lg shadow-md">
                Signup
            </button>
        </form>

        <p class="mt-4 text-center text-gray-600">
            Already have an account? <a href="/login" class="text-purple-500 hover:underline">Login</a>
        </p>
    </div>

</body>
</html>