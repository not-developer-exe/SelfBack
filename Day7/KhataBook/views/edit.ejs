<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Hisaab - Khaatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body class="bg-gray-900 text-white font-sans">
    <main class="w-full min-h-screen flex flex-col items-center px-6 py-10">
        
        <!-- Navbar -->
        <nav class="w-full max-w-5xl flex items-center justify-between px-6 py-4 bg-gray-800 shadow-lg rounded-lg">
            <h3 class="text-3xl font-bold text-blue-400">Khaatabook</h3>
            <a class="text-gray-300 hover:text-white transition" href="/">← Back to Home</a>
        </nav>

        <!-- Edit Hisab Form -->
        <div class="mt-10 w-full max-w-3xl p-6 bg-gray-800 rounded-lg shadow-lg">
            <h2 class="text-2xl font-semibold tracking-tight mb-6 text-blue-400">Edit Your Hisaab</h2>

            <!-- Show Error Message (If any) -->
            <% if (typeof error !== "undefined" && error) { %>
                <div class="mb-4 p-3 bg-red-500 text-white text-sm rounded-lg flex items-center">
                    <i class="ri-error-warning-line mr-2"></i> <%= error %>
                </div>
            <% } %>

            <form action="/edit/<%= filename %>" method="POST" class="space-y-4">
                <!-- Content Input -->
                <div>
                    <label for="content" class="block text-sm font-medium text-gray-300 mb-2">Edit Content</label>
                    <textarea id="content" name="content" rows="6" required
                        class="w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg focus:ring focus:ring-blue-400 outline-none"
                        placeholder="Edit your hisaab here..."><%= filedata %></textarea>
                </div>
            
                <!-- Password Input (Only if file is encrypted) -->
                <% if (isEncrypted) { %>
                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-300 mb-2">Password (Required to Save)</label>
                        <input type="password" id="password" name="password" required
                            class="w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-lg focus:ring focus:ring-blue-400 outline-none"
                            placeholder="Enter password to save changes..." />
                    </div>
                <% } %>
            
                <!-- Submit Button -->
                <button type="submit"
                    class="w-full px-4 py-2 bg-blue-500 hover:bg-blue-400 text-white font-semibold rounded-lg transition-all">
                    <i class="ri-save-line mr-2"></i> Save Changes
                </button>
            </form>
        </div>
    </main>
</body>
</html>