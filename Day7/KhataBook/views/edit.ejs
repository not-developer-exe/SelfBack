<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editing Hisaab</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body class="bg-gray-900 text-white font-sans">
    <main class="w-full h-screen flex flex-col items-center px-6 py-10">

        <!-- Navigation -->
        <nav class="flex w-full max-w-5xl justify-between items-center py-4 px-6 bg-gray-800 rounded-lg shadow-lg backdrop-blur-lg">
            <h3 class="text-3xl font-bold text-blue-400 tracking-tight">Khaatabook</h3>
            <div class="flex gap-6">
                <a href="/" class="text-gray-300 hover:text-white transition">Home</a>
                <a href="/create" class="text-gray-300 hover:text-white transition">+ New Hisaab</a>
            </div>
        </nav>

        <!-- Editing Section -->
        <div class="mt-10 w-full max-w-5xl px-6">

            <h3 class="text-2xl font-semibold text-gray-300 tracking-tight mb-6">You're editing hisaab of 
                <span class="text-blue-500"><%= filename.split('.')[0] %></span>
            </h3>

            <form action="/update/<%= filename %>" method="post" class="space-y-6">
                <textarea 
                    name="content"
                    class="block w-full px-4 py-3 bg-gray-800 border border-gray-600 rounded-md focus:ring-2 focus:ring-blue-500 resize-none min-h-[15rem] outline-none"
                    placeholder="Write your hisaab here..."><%= filedata %></textarea>
                
                <input 
                    type="submit" 
                    value="Update Hisaab" 
                    class="w-full px-5 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-500 transition duration-300">
            </form>
        </div>
    </main>
</body>

</html>
